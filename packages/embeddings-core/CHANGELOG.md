# @soat/embeddings-core

## 0.0.0-alpha.1

### Features

- Initial implementation of embeddings-core package
- Support for Ollama embedding provider
- Support for OpenAI embedding provider
- Environment-based configuration with `EMBEDDINGS_OLLAMA_MODEL` and `EMBEDDINGS_OPENAI_KEY`
