{
  "$schema": "./node_modules/turbo/schema.json",
  "concurrency": "4",
  "globalEnv": ["ENVIRONMENT"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "deploy": {
      "dependsOn": ["build", "test"],
      "env": ["AWS_*", "CARLIN_*"],
      "outputs": [".carlin/**"]
    }
  }
}
