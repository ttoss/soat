# List tutorials (by filename without extension) that should NOT be run in automated tests.

# One entry per line. Lines starting with # are comments. Blank lines are ignored.

# Example:

# connect-third-party-llms

index
connect-third-party-llms
# Mock providers + runner wiring are in place (tests/mocks/mock-providers.mjs,
# docker-compose.tutorials.yml). Still ignored only because the Ingestion Rules
# REST module + converter invocation are unimplemented (PRD Phases 2-3). Remove
# this entry once those land — no other change needed to run it end-to-end.
ingest-images-and-audio
